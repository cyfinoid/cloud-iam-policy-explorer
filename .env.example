# AWS Configuration for Test Scripts
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# Option 1: Use AWS Profile (RECOMMENDED)
# Uncomment and set your AWS CLI profile name
# AWS_PROFILE=your-profile-name

# Option 2: Use Direct Credentials (for testing only)
# Uncomment and set your AWS credentials
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token-if-using-temporary-credentials
# AWS_DEFAULT_REGION=us-east-1

# Option 3: Use uv virtual environment
# Set to 'true' to use .venv/bin/aws instead of system aws
USE_UV_VENV=false

# Test Account Prefix (optional)
# Change this to customize the prefix for test accounts
# TEST_PREFIX=test-shadow-admin
